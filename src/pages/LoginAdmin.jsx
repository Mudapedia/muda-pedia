import { Helmet } from "react-helmet";

const LoginAdmin = () => {
  return (
    <section>
      <Helmet>
        <title>Login Admin</title>
        <meta name="description" content="Helmet application" />
      </Helmet>
      <section>
        <h1>ini login untuk admin</h1>
      </section>
    </section>
  );
};

export default LoginAdmin;
