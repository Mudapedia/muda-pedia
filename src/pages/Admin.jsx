import { Helmet } from "react-helmet";

const Admin = () => {
  return (
    <section>
      <Helmet>
        <title>Dashboard</title>
      </Helmet>
      <section>
        <h1>ini halaman admin</h1>
      </section>
    </section>
  );
};

export default Admin;
